version: v1.0
name: Playwright Tests
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: Deployment
    task:
     jobs:
        - name: Running Playwright
          commands:
            - echo '--> OK this is playwright'
