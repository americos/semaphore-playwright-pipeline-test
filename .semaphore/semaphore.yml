version: v1.0
name: Playwright Tests
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Playwright Init
    task:
      jobs:
        - name: Playwright Setup
          commands:
            - echo '--> OK lets setup Playwright'
            - ls -la
            - node -v
            - npm -v
